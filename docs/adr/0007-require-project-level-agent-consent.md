# Require project-level agent consent

PiLot will require explicit, remembered consent before an agent first receives full command and file access in a project. After consent, runs proceed without per-command approval prompts; this preserves Pi's uninterrupted tool loop while ensuring that trusting project resources is not mistaken for authorizing agent execution.
